{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/udp_server",
            "args": [],
            "cwd": "${workspaceFolder}",
        // "preLaunchTask": "build"
        }
    ]
}
